# More powerful selection

Ie multi cell and section selection.

```js !plugin type=data id=fakePos
export const data = {
  start: {
    line: 1,
    column: 1,
    offset: 0
  },
  end: {
    line: 99,
    column: 99,
    offset: 999999
  }
}
```

```js !plugin id=selection type=menu
export const menu = (ctx, {React, Menu}) => {
  const pos = lit.file.data.plugins.data.fakePos
  const rc = React.createElement

  const selectAll = rc('span', {onClick: () => ctx.setSelectedCell(lit.ast.position)}, 'Select All')

  return rc( Menu, {
    title:"Selection",
    disabled: false,
  }, [selectAll])
}
```

```js
const {filter, select} = lit.utils.unist
const is = type => node => node.type === type

// return lit.ast
// return filter.toString()
return filter(lit.ast, is('root'))
```
```>txt attached=true updated=1620344481985
{ type: 'root',
  children: 
   [ { type: 'section',
       data: [Object],
       depth: 1,
       children: [Object],
       position: [Object] } ],
  position: 
   { start: { line: 1, column: 1, offset: 0 },
     end: { line: 87, column: 1, offset: 1809 } } }
```
