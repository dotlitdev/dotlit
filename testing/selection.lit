# More powerful selection

Ie multi cell and section selection.

```js !plugin type=data id=fakePos
export const data = {
  start: {
    line: 1,
    column: 1,
    offset: 0
  },
  end: {
    line: 99,
    column: 99,
    offset: 999999
  }
}
```
```js !plugin id=selection type=menu
export const menu = (ctx, {React, Menu}) => {
  const pos = lit.file.data.plugins.data.fakePos
  const rc = React.createElement
  const onClick = ctx => ev => {
    console.log("attempting to fake selection", pos)
    ctx.selectCell(pos)
    return false
  }
  return rc( Menu, {
    title:"Selection",
    disabled: false,
  }, [rc('span', {onClick: onClick(ctx)}, 'Select fakePos')])
}
```