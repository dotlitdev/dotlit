# meta/files_and_links.lit

## Unlinked files

```js > md
const noBacklinks = (n) => !n.backlinks || !n.backlinks.length;
const toMd = (n) => `- [${n.title || n.id}](${n.id})`

return fetch("/manifest.json").then((res) =>
  res.json().then((json) => {
    return "### unlinked\n\n" + json.nodes
               .filter(noBacklinks)
               .map(toMd)
               .join('\n');
  })
);

```
```>md attached=true updated=1623107342919
### unlinked


```
