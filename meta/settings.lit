# `.lit` Settings

```js !setting ns=lit
export const setting = {
  base: '/'
}
```

## GitHub 

```js !setting
export const setting = {
  github: {
    username: 'dotlitdev',
    repo: 'dotlit',
    branch: 'main',
    base: 'src/',
  }
}
```

## 🔌 Plug-ins

```js !plugin type=renderer
export const plugin = (...args) => {
  return (tree,file) => {
    file.data = file.data || {}
    file.data.__examplePluginActive = true
  }
}
```