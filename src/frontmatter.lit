# Frontmatter

<!-- data
Title: Frontmatter
Created at: some time in the past
Last Modified: recently
Author: An Utopian
-->

```jsx !plugin of=matter
export const viewer = ({node,React}) => {
  const matter = lit.utils.inspect(lit.file.data.frontmatter)
  return <div>{matter}</div>
}
```

```matter !inline

```