# testing/runkit.lit

experimenting using RunKit for custom repl.

```html !below
<!-- anywhere else on your page -->
<div id="my-element"></div>
```

```script !below https://embed.runkit.com

```

react bindings for simple embed: https://github.com/runkitdev/react-runkit


```js
const React = require('react')
const Embed = require('react-runkit')

const helloSource = `console.log('Hello, world!')`

module.exports = class HelloEmbed extends React.Component {
    render() {
        return <Embed source={ helloSource } />
    }
}
```