# Isomorphic Git

Since `.lit` already uses [[LightningFS]] for the local filesystem we can easily use https://isomorphic-git.org/docs/en/quickstart to manage versioning...

```js
return typeof lit.git
```
```>txt attached=true updated=1619303622481
object
```
```js
return (async () => {
  return lit.location.src + " : " + await lit.git.status({fs: lit.lfs, dir: lit.location.root, filepath: lit.location.src })
})()
```
```>txt attached=true updated=1619303217664
*added
```