# 🔬 Push, Pull and Extract

## Table of Contents 

## Push & Pull

Increase or decrease a sections nesting level.

```js
const {visit, selectPosition: selectAll} = lit.utils.unist
const {toMarkdown, ungroupSections}
      = lit.parser.utils
const unGroup = ungroupSections()()

const ast2md = tree => toMarkdown(unGroup(tree))

const tree = lit.ast
const test = 'heading'
const push = (node) => node.depth += 1 
const pull = (node) => node.depth -= 1

return selectPosition.selectAll
visit(tree,test,push)
return ast2md(tree)
```
```>txt attached=true updated=1624574041719
{ atPos: [Getter], selectAll: [Getter] }
```




## Extract