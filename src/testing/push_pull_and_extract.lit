# 🔬 Push, Pull and Extract

## Table of Contents 

## Push & Pull

Increase or decrease a sections nesting level.

```js
const {visit} = lit.utils.unist
const {toMarkdown, ungroupSections}
      = lit.parser.utils
const unGroup = ungroupSections()()

const ast2md = tree => toMarkdown(unGroup(tree))

const tree = lit.ast
const test = 'heading'
const push = (node) => node.depth += 1 
const pull = (node) => node.depth -= 1

visit(tree,test,push)
return ast2md(tree)
```
```>txt attached=true updated=1624573345289
## 🔬 Push, Pull and Extract

### Table of Contents

*   [Push & Pull](#push--pull)
*   [Extract](#extract)

### Push & Pull

Increase or decrease a sections nesting level.

•••js
const {visit} = lit.utils.unist
const {toMarkdown, ungroupSections}
      = lit.parser.utils
const unGroup = ungroupSections()()

const ast2md = tree => toMarkdown(unGroup(tree))

const tree = lit.ast
const test = 'heading'
const push = (node) => node.depth += 1 
const pull = (node) => node.depth -= 1

visit(tree,test,push)
return ast2md(tree)
•••

### Extract

```




## Extract