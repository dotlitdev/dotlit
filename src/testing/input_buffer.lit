# Input Buffer

- [ ] On load check for url query string `input`.
  - [ ] If `input` is available, decode markdown.
  - [ ] Check document for section corresponding to current date, if available append `input` other wise create with `input`.
- [ ]
- [ ]

```js !plugin type=onload
export const onload = () => {
  console.log(location)
}
```