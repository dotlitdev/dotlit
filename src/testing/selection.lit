# More powerful selection

Ie multi cell and section selection.

```js !plugin type=data id=fakePos
export const data = {
  start: {
    line: 1,
    column: 1,
    offset: 0
  },
  end: {
    line: 99,
    column: 99,
    offset: 999999
  }
}
```

```js !plugin id=selection type=menu
export const menu = (ctx, {React, Menu}) => {
  const pos = lit.file.data.plugins.data.fakePos
  const rc = React.createElement

  const selectAll = rc('span', {onClick: () => ctx.setSelectedCell(lit.ast.position)}, 'Select All')

  return rc( Menu, {
    title:"Selection",
    disabled: false,
  }, [selectAll])
}
```

```js
const {filter, select, selectPosition} = lit.utils.unist
const is = t => node => {
  console.log(node.type)
  return node.type === t
}

// return lit.ast
// return filter.toString()
// filter(lit.ast, {cascade: false } , is('heading'))
return lit.notebook.state
```
```>txt attached=true updated=1620345369081
root
{ type: 'cell',
  position: 
   { start: { line: 1, column: 1, offset: 0 },
     end: { line: 3, column: 37, offset: 63 } },
  data: 
   { hName: 'cell',
     hProperties: { class: 'cell' },
     section: { id: undefined, position: [Object] } },
  children: 
   [ { type: 'heading',
       depth: 1,
       children: [Object],
       position: [Object],
       data: [Object],
       processed: true },
     { type: 'paragraph', children: [Object], position: [Object] } ] }
```
