# [[🔬 Testing]] Section grouping

Pseodo code steps:

1. With a stack initialised with root node (with children romoved) ie `[root]`
1. define `currentSection` as `null`
1. Iterate over all removed root node chilren (in reverse? TBD) as `node`
  1. if `currentSection` is null
    1. ceate with node depth or 0.
  1. if `node` is a `heading`
    1. with deeper depth?
    1. with shallower depth?
    1. with equal depth?
  1. add to `currentSection`