# Importing JS Modules

```js #destructuring
const x = {
  default: "a",
  b: "42",
  c: () => {},
};
const { default: y } = x;
return { y };

```

```js #skypack
const name = "canvas-confetti";
const endpoint = "https://cdn.skypack.dev/";
const module = import(`${endpoint}${name}`);
return module;

```
```>txt attached=true updated=1621547815618
{ create: [Function: confettiCannon],
  default: { [Function: fire] reset: [Function], create: [Function: confettiCannon] } }
```

```js #unpkg
const name = 'canvas-confetti'
const endpoint = 'https://unpkg.com/'
return import(`${endpoint}${name}`)
```

