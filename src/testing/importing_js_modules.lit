# Importing JS Modules

```js #destructuring
const x = {
  default: "a",
  b: "42",
  c: () => {},
};
const { default: y } = x;
return { y };

```

```lang

```


```js #skypack
const skypack = (package) => import(`https://cdn.skypack.dev/${package}`);

return skypack("canvas-confetti");

```


```js #unpkg
const unpkg = (package) => import(`https://unpkg.com/${package}?module`);

return unpkg("canvas-confetti");

```

