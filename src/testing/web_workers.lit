# testing/Web Workers

[Using Web Workers on MDN](https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Using_web_workers)
```js
copy me.
```


```js
return `Worker supported: ${!!window.Worker}`
```
```>txt attached=true updated=1619529490906
Worker supported: true
```
```>js worker.js #implementation
// postMessage("hi")
console.log("anyone here?")
```
Check if web server has content yet?

```js
return (async fn => {
  const resp = await fetch('./worker.js')
  return resp.status + (await resp.text())
})()
```
```>txt attached=true updated=1619532278259
200postMessage("hi")
```
Current REPL results in script execution from cross-origin? Due to data:uri

```js
return new Promise((resolve,reject) => {
  let myWorker;
  try {
    myWorker = new Worker('worker.js')
    myWorker.onmessage = resolve
    myWorker.onerror = (err) => {
      resolve({msg: "worker.onerror", err})
    }
  } catch(err) {
    resolve({msg: "Caught err", err})
  }
  
})
```
```>txt attached=true updated=1619531217657
{ isTrusted: [Getter] }
```
```js
return new Promise((resolve,reject) => {
  let myWorker;
  try {
    myWorker = new Worker('https://dotlit.org/testing/worker.js')
    myWorker.onmessage = resolve
    myWorker.onerror = (err) => {
      resolve({msg: "worker.onerror", err})
    }
  } catch(err) {
    resolve({msg: "Caught err", err})
  }
  
})
```
```>txt attached=true updated=1619532157229
{ isTrusted: [Getter] }
```