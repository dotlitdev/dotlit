# Fuzzy text search

```js
import('https://cdn.skypack.dev/fuzzyset').then( FuzzySet => {

  const f = new FuzzySet()
  f.add("the text of mine")
  f.add("the text of someone else")
  f.add("other texts")
  return f.get("my text")
})

```