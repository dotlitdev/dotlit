# Compact Manifest

Reducing the serialised size of a manifest of all files in a `.lit` notebook.

```js
return (async fn => {
  const t = await import('https://cdn.skypack.dev/compact-prefix-tree')
  return t
})()
```