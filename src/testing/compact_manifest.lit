# Compact Manifest

Reducing the serialised size of a manifest of all files in a `.lit` notebook.

```js
return (async fn => {
  const {CompactPrefixTree as tree} = await import('https://cdn.skypack.dev/compact-prefix-tree')
  return tree
})()
```
```>txt attached=true updated=1621378443532
{ CompactPrefixTree: [Function: CompactPrefixTree],
  add: [Function: add],
  default: [Function: CompactPrefixTree],
  getPrefix: [Function: getPrefix],
  getWordsFromTrie: [Function: getWordsFromTrie] }
```