# Testing local and remote files

```js 
var n = Date.now()
var d = new Date()
console.log(n, d, Date(n), Date(d), d.getTime())
```


The file you were looking for does not *yet* exist. Edit it this page, to change that.