# meta/files_and_links.lit

## Unlinked files

```js
const noBacklinks = n => n.backlinks || !n.backlinks.length
const getTitle = n => n.title || n.id 

return fetch('/manifest.json').then(res => res.json().then(json => {
  return json.nodes
         .filter(noBacklinks)
         .map(getTitle)
})
```
